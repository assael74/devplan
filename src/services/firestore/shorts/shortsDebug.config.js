// src/services/firestore/shorts/shortsDebug.config.js
export const SHORTS_DEBUG = {
  // ✅ האם להדפיס לוגים
  enabled: false,

  // ✅ האם לדלג על כתיבה ל-Firestore (dry-run)
  dryRun: false,

  logPayload: true,
  logListSize: true,
  logObjects: true,
}
